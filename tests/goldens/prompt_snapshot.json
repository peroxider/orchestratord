{
  "clarification_block_args": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\nPRE-RENDERED CLARIFICATION BLOCK\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "clarification_context_answer": "---\n## Clarification Context\n\n\nThe issue author or operator supplied clarification before this run. Treat the\nanswer below as part of the issue requirements.\n\n- Question: \"Pre-dispatch clarification\"\n- Answer (issue author): \"3600 seconds\"\n\n---",
  "clarification_context_none": "",
  "clarification_context_pending": "---\n## Clarification Context\n\n\nThis issue is currently awaiting clarification. When the answer is available,\nit will be provided below. If you are unsure about any aspect of the issue,\nuse the `AskIssueAuthor` tool to request clarification from the issue author\nor local operator.\n\nWhen requesting clarification:\n- Be specific: ask exactly what is ambiguous (e.g., \"Should this function be sync or async?\")\n- Provide context: include relevant code snippets or error messages\n- Limit to one question at a time to avoid overwhelming responders\n\n- Current pending question: \"Which TTL value?\"\n\n- Available options: 3600, 86400\n\n\n\n---",
  "conflict_files": "---\n## Conflicting Files (rebase reentry)\n\nThe orchestrator's automated rebase left the following files in a\nconflict state (REBASE_HEAD is set in the workspace). Read each\nfile, resolve the conflict markers (`<<<<<<<`, `=======`,\n`>>>>>>>`), then continue the rebase and push:\n\n- `src/auth/session.py`\n- `src/auth/keys.py`\n\nSuggested commands (run from the workspace root):\n\n```bash\ngit status              # confirm REBASE_HEAD state\n# Edit each file above to remove conflict markers.\ngit add <resolved files>\ngit rebase --continue\ngit push --force-with-lease=origin/<branch>:<remote_sha> \\\n    origin <branch>\n```\n---\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "feedback_summary": "## Orchestratord PR Review Follow-up Summary\n\n**Follow-up attempt**: #2\n**Processed**: 1 item(s)\n\n### Auto-handled\n- [inline_review] FB-1 (`src/auth/session.py:42`): Please add a regression test for the cookie TTL.\n\n### Needs human attention\n- [ci] FB-2: unclear request",
  "generic_task": "You are an autonomous software engineering agent.\n\nTask: Stage: implement parser\n\n\nDescription:\nParse the token stream into an AST.\n\n\nPriority: 1\n\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "issue_task_basic": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "kitchen_sink": "---\n## Conflicting Files (rebase reentry)\n\nThe orchestrator's automated rebase left the following files in a\nconflict state (REBASE_HEAD is set in the workspace). Read each\nfile, resolve the conflict markers (`<<<<<<<`, `=======`,\n`>>>>>>>`), then continue the rebase and push:\n\n- `src/auth/keys.py`\n\nSuggested commands (run from the workspace root):\n\n```bash\ngit status              # confirm REBASE_HEAD state\n# Edit each file above to remove conflict markers.\ngit add <resolved files>\ngit rebase --continue\ngit push --force-with-lease=origin/<branch>:<remote_sha> \\\n    origin <branch>\n```\n---\n\n\u26d4 **\u7ea6\u675f\u63d0\u9192**\uff1a\u59cb\u7ec8\u7528 `/opt/venv/bin/python3` \u7edd\u5bf9\u8def\u5f84\u8fd0\u884c Python\uff0c\u4e0d\u8981\u8c03\u8bd5\u73af\u5883\u5dee\u5f02\u3002\n\n---\n## \u4e0a\u6b21\u8fd0\u884c\u7684\u9a8c\u8bc1\u5931\u8d25\uff08\u5fc5\u987b\u5904\u7406\uff09\n\n\u4e0a\u6b21\u63d0\u4ea4\u524d\u7684\u68c0\u67e5\uff08pre-commit / \u9a8c\u8bc1\u547d\u4ee4\uff09\u5931\u8d25\uff0c\u8f93\u51fa\u5982\u4e0b\u2014\u2014\n\u8bf7\u5148\u9488\u5bf9\u5931\u8d25\u539f\u56e0\u4fee\u6b63\u4ee3\u7801\uff0c\u786e\u4fdd\u68c0\u67e5\u901a\u8fc7\u540e\u518d\u63d0\u4ea4\uff1a\n\n```\nFAILED tests/test_auth.py::test_ttl\n```\n---\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "legacy_issue_object": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\n\nDescription:\nSession cookie expires too early.\n\n\nPriority: 2\n\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "legacy_issue_object_with_attempt": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\n\nDescription:\nSession cookie expires too early.\n\n\nPriority: 2\n\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "operator_hints": "---\n## Operator Hints\n\nPrefer pytest fixtures over manual setup.\n---\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n## Premise Check (IMPORTANT)\n\nThe issue references the following paths, but they do **not** exist\nin this repository:\n\n- `src/auth/session.py`\n- `docs/auth.md`\n\nBefore making any change, verify the issue's premise:\n\n1. Search the repository for the described file/symbol/behavior\n   (it may have moved or been renamed).\n2. If the issue describes a bug in code that does not exist, the\n   bug is unverifiable. **Do NOT create the missing file or invent\n   code to make the fix look plausible** \u2014 fabricated fixes are\n   worse than no fix.\n3. If you conclude the task cannot honestly be completed, write a\n   JSON file at `.orchestrator_control/cannot_proceed.json` shaped like:\n\n   ```json\n   {\"reason\": \"premise_not_met\",\n    \"details\": \"src/foo.py referenced by the issue does not exist; searched for <symbol> with no results\",\n    \"checked\": [\"src/foo.py\", \"git log\", \"grep _RetryStrategy\"]}\n   ```\n\n   then stop without modifying source files. The orchestrator will\n   report your findings back to the issue author instead of opening\n   a merge request.\n\nOnly proceed with code changes if you can locate the real code the\nissue is talking about.\n---\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "premise_missing_files": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nFunction gcd in gcd.py crashes on zero; also see existing.py.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n## Premise Check (IMPORTANT)\n\nThe issue references the following paths, but they do **not** exist\nin this repository:\n\n- `gcd.py`\n\nBefore making any change, verify the issue's premise:\n\n1. Search the repository for the described file/symbol/behavior\n   (it may have moved or been renamed).\n2. If the issue describes a bug in code that does not exist, the\n   bug is unverifiable. **Do NOT create the missing file or invent\n   code to make the fix look plausible** \u2014 fabricated fixes are\n   worse than no fix.\n3. If you conclude the task cannot honestly be completed, write a\n   JSON file at `.orchestrator_control/cannot_proceed.json` shaped like:\n\n   ```json\n   {\"reason\": \"premise_not_met\",\n    \"details\": \"src/foo.py referenced by the issue does not exist; searched for <symbol> with no results\",\n    \"checked\": [\"src/foo.py\", \"git log\", \"grep _RetryStrategy\"]}\n   ```\n\n   then stop without modifying source files. The orchestrator will\n   report your findings back to the issue author instead of opening\n   a merge request.\n\nOnly proceed with code changes if you can locate the real code the\nissue is talking about.\n---\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "previous_run_ids": "---\n## Previous Attempts\n\nThis issue has been attempted before and failed.  You can inspect\nthe full conversation transcript of each previous run to understand\nwhat was tried, what went wrong, and what to avoid this time.\n\n- `run-AAA` \u2014 `Read(path=\"sessions-home/run-AAA/transcript.jsonl\")`\n- `run-BBB` \u2014 `Read(path=\"sessions-home/run-BBB/transcript.jsonl\")`\n---\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "python_executable": "\u26d4 **\u7ea6\u675f\u63d0\u9192**\uff1a\u59cb\u7ec8\u7528 `/opt/venv/bin/python3` \u7edd\u5bf9\u8def\u5f84\u8fd0\u884c Python\uff0c\u4e0d\u8981\u8c03\u8bd5\u73af\u5883\u5dee\u5f02\u3002\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "rebase_prompt": "---\n# PR Conflict Resolution \u2014 ISSUE-7\n\n**Title:** Fix login cookie expiry\n**Branch:** `fix/cookie-ttl` (base `master`)\n\n## Reason\n\nupstream force-push\n\n## Task\n\nThe orchestrator's automated `git rebase origin/<base>` left this\nbranch with content conflicts. Your job is to resolve each conflict,\ncontinue the rebase, and push the rebased branch with\n`--force-with-lease` (the default) so the PR becomes mergeable\nagain. **Do NOT close the PR or open a new one.**\n\n## Conflicting Files\n\n- `src/auth/session.py`\n- `src/auth/keys.py`\n\n## Procedure\n\n1. `git status` \u2014 confirm REBASE_HEAD is set.\n2. For each file above: read the file, remove the\n   `<<<<<<<`/`=======`/`>>>>>>>` markers, write the merged\n   content you want kept.\n3. `git add <file>` for each resolved file.\n4. `git rebase --continue` (or `--skip` if the upstream commit is\n   the one to drop \u2014 but only when clearly safe).\n5. `git log --oneline -5` to verify the rebased history.\n6. Capture the new `HEAD` SHA, then push:\n\n   ```bash\n   REMOTE_SHA=$(git rev-parse origin/<branch>)\n   git push --force-with-lease=<branch>:$REMOTE_SHA origin <branch>\n   ```\n\n7. Print the final head SHA in your response so the orchestrator\n   can record it.\n\n## Constraints\n\n- **Do not** run `git rebase --abort` unless explicitly asked; we\n  want the rebased history, not the pre-rebase one.\n- **Do not** use plain `git push --force`; the orchestrator\n  defaults to `--force-with-lease` to avoid clobbering concurrent\n  pushes. Only use `--force` if the operator explicitly passed\n  `--force` to the rebase CLI.\n- **Do not** open a new PR; the existing PR will pick up the\n  rebased head automatically once the push lands.\n---",
  "rebase_prompt_no_conflicts": "---\n# PR Conflict Resolution \u2014 ISSUE-7\n\n**Title:** Fix login cookie expiry\n**Branch:** `fix/cookie-ttl` (base `master`)\n\n## Task\n\nThe orchestrator's automated `git rebase origin/<base>` left this\nbranch with content conflicts. Your job is to resolve each conflict,\ncontinue the rebase, and push the rebased branch with\n`--force-with-lease` (the default) so the PR becomes mergeable\nagain. **Do NOT close the PR or open a new one.**\n\n## Conflicting Files\n\n- (no specific files reported \u2014 run `git diff --name-only --diff-filter=U` to list them)\n\n## Procedure\n\n1. `git status` \u2014 confirm REBASE_HEAD is set.\n2. For each file above: read the file, remove the\n   `<<<<<<<`/`=======`/`>>>>>>>` markers, write the merged\n   content you want kept.\n3. `git add <file>` for each resolved file.\n4. `git rebase --continue` (or `--skip` if the upstream commit is\n   the one to drop \u2014 but only when clearly safe).\n5. `git log --oneline -5` to verify the rebased history.\n6. Capture the new `HEAD` SHA, then push:\n\n   ```bash\n   REMOTE_SHA=$(git rev-parse origin/<branch>)\n   git push --force-with-lease=<branch>:$REMOTE_SHA origin <branch>\n   ```\n\n7. Print the final head SHA in your response so the orchestrator\n   can record it.\n\n## Constraints\n\n- **Do not** run `git rebase --abort` unless explicitly asked; we\n  want the rebased history, not the pre-rebase one.\n- **Do not** use plain `git push --force`; the orchestrator\n  defaults to `--force-with-lease` to avoid clobbering concurrent\n  pushes. Only use `--force` if the operator explicitly passed\n  `--force` to the rebase CLI.\n- **Do not** open a new PR; the existing PR will pick up the\n  rebased head automatically once the push lands.\n---",
  "render_parts_no_marker": "SYSTEM<<<>>>USER<<<You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n--->>>",
  "review_feedback": "You are an autonomous software engineering agent fixing pull request feedback.\n\nIssue: ISSUE-7 - Fix login cookie expiry\nPull request: #7 (https://example.test/pr/7)\nBranch: fix/cookie-ttl\n\nCurrent task:\n- Fix only the PR review feedback and CI failures listed below.\n- Do not expand scope or reimplement unrelated issue requirements.\n- Work on the current branch only; do not create a new branch or pull request.\n- Prefer the smallest correct change that addresses the feedback.\n- If feedback is conflicting or unclear, leave code unchanged for that item and explain what clarification is needed.\n- Run relevant tests or record why they cannot be run.\n- CLI Usage: when suggesting terminal commands, use `orchestratord` not `python3 -c` or `PYTHONPATH=`.\n\nFeedback:\n\n1. [inline_review] FB-1 severity=warning status=open\n   File: src/auth/session.py:42\n   Commit: abc1234\n   URL: https://example.test/pr/7#discussion-1\n   Diff hunk:\n```diff\n@@ -40,7 +40,7 @@\n-    ttl = 60\n+    ttl = 3600\n```\n   Body:\nPlease add a regression test for the cookie TTL.\n\n2. [ci] FB-2 severity=error status=open\n   Body:\nLint failure: unused import in worker.py",
  "sequential_workspace": "You are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n## Sequential Workspace Context\n\nThis issue is running in a sequential shared workspace.\n- Workspace strategy: `sequential`\n- Integration branch: `integration`\n- Start commit: `abc1234`\n- Base commit: `def5678`\n- Previous issue: `ISSUE-6`\n- Sequence index: `3`\n\nBuild on the existing commit chain in this workspace. Do not redo earlier issues.\nIf the expected prior commit chain appears to be missing, stop and report it.\n---\n\n---\n## Premise Check (IMPORTANT)\n\nThe issue references the following paths, but they do **not** exist\nin this repository:\n\n- `src/auth/session.py`\n- `docs/auth.md`\n\nBefore making any change, verify the issue's premise:\n\n1. Search the repository for the described file/symbol/behavior\n   (it may have moved or been renamed).\n2. If the issue describes a bug in code that does not exist, the\n   bug is unverifiable. **Do NOT create the missing file or invent\n   code to make the fix look plausible** \u2014 fabricated fixes are\n   worse than no fix.\n3. If you conclude the task cannot honestly be completed, write a\n   JSON file at `.orchestrator_control/cannot_proceed.json` shaped like:\n\n   ```json\n   {\"reason\": \"premise_not_met\",\n    \"details\": \"src/foo.py referenced by the issue does not exist; searched for <symbol> with no results\",\n    \"checked\": [\"src/foo.py\", \"git log\", \"grep _RetryStrategy\"]}\n   ```\n\n   then stop without modifying source files. The orchestrator will\n   report your findings back to the issue author instead of opening\n   a merge request.\n\nOnly proceed with code changes if you can locate the real code the\nissue is talking about.\n---\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "stage_task": "You are an autonomous software engineering agent.\n\nTask: Stage: implement parser\n\n\nDescription:\nParse the token stream into an AST.\n\n\nPriority: 1\n\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---",
  "verification_error": "---\n## \u4e0a\u6b21\u8fd0\u884c\u7684\u9a8c\u8bc1\u5931\u8d25\uff08\u5fc5\u987b\u5904\u7406\uff09\n\n\u4e0a\u6b21\u63d0\u4ea4\u524d\u7684\u68c0\u67e5\uff08pre-commit / \u9a8c\u8bc1\u547d\u4ee4\uff09\u5931\u8d25\uff0c\u8f93\u51fa\u5982\u4e0b\u2014\u2014\n\u8bf7\u5148\u9488\u5bf9\u5931\u8d25\u539f\u56e0\u4fee\u6b63\u4ee3\u7801\uff0c\u786e\u4fdd\u68c0\u67e5\u901a\u8fc7\u540e\u518d\u63d0\u4ea4\uff1a\n\n```\nFAILED tests/test_auth.py::test_ttl\n```\n---\n\nYou are an autonomous software engineering agent.\n\nTask: Fix login cookie expiry\n\nIssue: ISSUE-7\n\n\nDescription:\nSession cookie expires too early. See src/auth/session.py and docs/auth.md.\n\n\nPriority: 2\n\n\nState: opened\n\n\nPlease analyze the issue, implement the necessary changes, and ensure all tests pass.\n\n## CLI Usage Guidelines\nWhen you need to suggest terminal commands for the user:\n- Always use the `orchestratord` CLI entrypoint, NOT `python3 -c` or `PYTHONPATH=`.\n- For orchestrator status: `orchestratord server status`\n- For issue list: `orchestratord issue list`\n- For issue tail: `orchestratord issue tail --id <id>`\n- For other commands: use `orchestratord --help`\n\n---\n\n\nTask tracking guidelines:\n- For multi-step work, create structured tasks with TaskCreate and track them with TaskList.\n- Declare dependencies with TaskUpdate `addBlockedBy` before starting work.\n- Only mark a task `in_progress` after TaskGet confirms its `blockedBy` list is empty.\n- Only mark a task `completed` when the work is fully done and tests pass.\n- If a task becomes blocked, use TaskList/TaskGet to read the blocked reason and repair suggestions, then act on them.\n---"
}